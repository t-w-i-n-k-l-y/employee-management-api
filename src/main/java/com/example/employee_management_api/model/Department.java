package com.example.employee_management_api.model;

/**
 * Enum representing the various departments within the organization
 * to ensures only valid department names are used.
 */
public enum Department {
    HR,
    ENGINEERING,
    MARKETING,
    SALES
}
